// This file is generated. Edit build/generate-shaders.ts, then run `npm run codegen`.
export default 'in vec4 v_color;in highp vec2 v_wall_uv;in highp float v_height_m;in lowp float v_is_side;void main() {fragColor=v_color;if (v_is_side > 0.5 && v_height_m >=5.0) {float num_floors=max(1.0,floor(v_height_m/3.5));float floor_v=fract(v_wall_uv.y*num_floors);float band_b=0.18;float band_t=0.78;float fw_v=fwidth(floor_v);float floor_mask=smoothstep(band_b-fw_v,band_b+fw_v,floor_v)*smoothstep(band_t+fw_v,band_t-fw_v,floor_v);float fw_floor=fwidth(v_wall_uv.y*num_floors);float floor_detail=1.0-smoothstep(0.15,0.45,fw_floor);float band_mask=mix(1.0,floor_mask,floor_detail);float window_spacing=600.0;float raw_u=v_wall_uv.x/window_spacing;float cell_u=fract(raw_u);float fw_u=fwidth(raw_u);float win_l=0.20;float win_r=0.80;float detail=1.0-smoothstep(0.04,0.12,fw_u);float col_mask=mix(1.0,smoothstep(win_l-fw_u,win_l+fw_u,cell_u)*smoothstep(win_r+fw_u,win_r-fw_u,cell_u),detail);float win_mask=band_mask*col_mask;float lod_tint=max(detail,floor_detail);win_mask*=lod_tint;if (win_mask > 0.01) {vec2 grid_id=floor(vec2(raw_u,v_wall_uv.y*num_floors));float hash=fract(sin(dot(grid_id,vec2(12.9898,78.233)))*43758.5453);vec3 window_color=vec3(0.55,0.78,0.90)+hash*vec3(-0.04,-0.02,0.02);if (detail > 0.5) {float local_u=clamp((cell_u-win_l)/(win_r-win_l),0.0,1.0);float local_v=clamp((floor_v-band_b)/(band_t-band_b),0.0,1.0);float diag=(local_u+local_v)*0.7;float fw_diag=fwidth(diag);float glare=smoothstep(0.3-fw_diag,0.5,diag)*smoothstep(0.7+fw_diag,0.5,diag);glare*=0.50+hash*0.15;window_color=mix(window_color,vec3(1.0),glare);}float luminance=dot(v_color.rgb,vec3(0.299,0.587,0.114));vec3 lit_window=window_color*max(luminance*1.2,0.60);fragColor.rgb=mix(fragColor.rgb,lit_window,0.75*win_mask);}}if (v_is_side > 0.5) {float base_ao=smoothstep(0.0,0.10,v_wall_uv.y);fragColor.rgb*=mix(0.86,1.0,base_ao);float top_glow=smoothstep(0.92,1.0,v_wall_uv.y);fragColor.rgb*=mix(1.0,1.06,top_glow);}\n#ifdef OVERDRAW_INSPECTOR\nfragColor=vec4(1.0);\n#endif\n}';
